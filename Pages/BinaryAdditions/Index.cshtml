@page
@model OnlineBinaryExpressionEvaluator.Pages.BinaryAdditions.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.BinaryAddition[0].Operand1)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.BinaryAddition[0].Operand2)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.BinaryAddition[0].AdditionResult)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.BinaryAddition) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Operand1)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Operand2)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AdditionResult)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
